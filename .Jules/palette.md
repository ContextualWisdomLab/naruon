## 2024-05-23 - title attributes for disabled buttons

**Learning:** Buttons in this repository that have `disabled` attribute should have a `title` explaining why it's disabled. This is useful for screen readers and tooltips.
**Action:** Added `title` attribute for disabled buttons to improve accessibility.
