[pytest]
asyncio_default_fixture_loop_scope = function
# Do not auto-load LangSmith's third-party pytest plugin: importing
# langsmith.client evaluates asyncio.iscoroutinefunction() at class-definition
# time, which raises DeprecationWarning under Python 3.14 + PYTHONWARNINGS=error
# and aborts pytest before ini filterwarnings can apply. This repo never uses
# @pytest.mark.langsmith; remove this block once langsmith ships the upstream
# inspect.iscoroutinefunction fix.
addopts =
    -p no:langsmith_plugin
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*:starlette.exceptions.StarletteDeprecationWarning
    ignore:You are using a Python version.*which Google will stop supporting.*:FutureWarning
    ignore:Unclosed <MemoryObject:ResourceWarning
    ignore:pkg_resources is deprecated:UserWarning
    ignore:pkg_resources is deprecated:DeprecationWarning
    ignore:Deprecated call to .pkg_resources.declare_namespace:DeprecationWarning
    ignore:builtin type SwigPyPacked has no __module__ attribute:DeprecationWarning
    ignore:builtin type SwigPyObject has no __module__ attribute:DeprecationWarning
    ignore:builtin type swigvarlink has no __module__ attribute:DeprecationWarning
markers =
    postgres: requires a reachable PostgreSQL DATABASE_URL
