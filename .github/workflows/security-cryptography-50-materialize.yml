name: Materialize cryptography 50 security remediation

on:
  pull_request:
    branches:
      - develop
    types: [opened, reopened, synchronize]

permissions:
  contents: read

concurrency:
  group: security-cryptography-50-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  materialize:
    if: ${{ github.event.pull_request.head.ref == 'fix/cryptography-50-bleichenbacher' }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write
    env:
      PYTHONWARNINGS: error
      DISABLE_BACKGROUND_WORKERS: "1"
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit

      - name: Checkout remediation branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: fix/cryptography-50-bleichenbacher
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "0.11.29"

      - name: Install supported Python runtimes
        run: uv python install 3.13 3.14

      - name: Detect whether materialization is required
        id: state
        run: |
          if grep -Fq 'cryptography==50.0.0' backend/requirements.txt; then
            echo 'required=false' >> "$GITHUB_OUTPUT"
          else
            echo 'required=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Add and prove the failing security contract
        if: ${{ steps.state.outputs.required == 'true' }}
        run: |
          set -euo pipefail
          python .github/scripts/materialize_cryptography_50.py add-test
          set +e
          (
            cd backend
            uv run --frozen python -m pytest -q \
              tests/test_release_governance.py::test_cryptography_runtime_pins_are_bleichenbacher_oracle_fixed
          )
          red_status=$?
          set -e
          if [ "$red_status" -eq 0 ]; then
            echo "::error::The security regression test unexpectedly passed before remediation."
            exit 1
          fi

      - name: Update governed security constraints and changelog
        if: ${{ steps.state.outputs.required == 'true' }}
        run: python .github/scripts/materialize_cryptography_50.py apply

      - name: Regenerate uv and pip hash locks
        if: ${{ steps.state.outputs.required == 'true' }}
        run: |
          set -euo pipefail
          (
            cd backend
            uv lock --upgrade-package cryptography
            uv lock --check
          )
          uv pip compile \
            --generate-hashes \
            --python-version 3.14 \
            --python-platform x86_64-manylinux_2_28 \
            --upgrade-package cryptography \
            --output-file backend/requirements-hashes.txt \
            backend/requirements.txt
          uv pip compile \
            --generate-hashes \
            --python-version 3.13 \
            --python-platform x86_64-manylinux_2_28 \
            --upgrade-package cryptography \
            --output-file requirements-strix-ci-hashes.txt \
            requirements-strix-ci.txt

      - name: Verify deterministic locks and focused governance
        if: ${{ steps.state.outputs.required == 'true' }}
        run: |
          set -euo pipefail
          ! grep -R -F 'cryptography==49.0.0' \
            backend/requirements.txt \
            backend/pyproject.toml \
            backend/uv.lock \
            backend/requirements-hashes.txt \
            requirements-strix-ci.txt \
            requirements-strix-ci-hashes.txt \
            backend/tests/test_release_governance.py
          (
            cd backend
            uv run --locked ruff check --fix tests/test_release_governance.py
            uv run --locked ruff format tests/test_release_governance.py
            uv run --locked ruff check tests/test_release_governance.py
            uv run --locked ruff format --check tests/test_release_governance.py
            uv run --locked python -m pytest -q tests/test_release_governance.py
          )

      - name: Verify hash-locked backend installation
        if: ${{ steps.state.outputs.required == 'true' }}
        run: |
          set -euo pipefail
          uv venv --python 3.14 .venv-security-backend
          uv pip install \
            --python .venv-security-backend/bin/python \
            --require-hashes \
            -r backend/requirements-hashes.txt
          .venv-security-backend/bin/python -c \
            'import cryptography; assert cryptography.__version__ == "50.0.0"'

      - name: Verify hash-locked Strix CI installation
        if: ${{ steps.state.outputs.required == 'true' }}
        run: |
          set -euo pipefail
          uv venv --python 3.13 .venv-security-strix
          uv pip install \
            --python .venv-security-strix/bin/python \
            --require-hashes \
            -r requirements-strix-ci-hashes.txt
          .venv-security-strix/bin/python -c \
            'import cryptography; assert cryptography.__version__ == "50.0.0"'

      - name: Run the full backend regression suite
        if: ${{ steps.state.outputs.required == 'true' }}
        run: cd backend && uv run --locked python -m pytest -q

      - name: Commit verified remediation
        if: ${{ steps.state.outputs.required == 'true' }}
        run: |
          set -euo pipefail
          rm -rf .venv-security-backend .venv-security-strix
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            CHANGELOG.md \
            backend/pyproject.toml \
            backend/uv.lock \
            backend/requirements.txt \
            backend/requirements-hashes.txt \
            backend/tests/test_release_governance.py \
            requirements-strix-ci.txt \
            requirements-strix-ci-hashes.txt
          git commit -m "fix(security): upgrade cryptography to 50.0.0"
          git push origin HEAD:fix/cryptography-50-bleichenbacher
