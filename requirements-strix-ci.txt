strix-agent==1.5.3
google-cloud-aiplatform==1.165.1
# strix-agent 1.5.3 declares cryptography<49, but its installed source does
# not import cryptography directly. The central Strix installer uses this
# hash-locked security override with --no-deps; keep the fixed runtime pin.
cryptography==50.0.0
protobuf==6.33.6
python-multipart==0.0.32
